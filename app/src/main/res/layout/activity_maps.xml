<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:map="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
<fragment
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/map"
    android:name="com.google.android.gms.maps.SupportMapFragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_above="@+id/latitude_edit_text"
    tools:context="tutorial.android.endava.androidtutorial.chapter_travel.MapsActivity" />
    <Button
        android:id="@+id/change_terrain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <EditText
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:inputType="numberSigned"

        android:id="@+id/latitude_edit_text"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true" />

    <EditText
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:inputType="numberSigned"
        android:id="@+id/longitude_edit_text"
        android:layout_alignTop="@+id/latitude_edit_text"
        android:layout_toEndOf="@+id/latitude_edit_text" />

    <Button
        android:id="@+id/add_marker_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/latitude_edit_text"
        android:layout_toEndOf="@+id/longitude_edit_text"
        android:text="@string/add_marker"/>

</RelativeLayout>
